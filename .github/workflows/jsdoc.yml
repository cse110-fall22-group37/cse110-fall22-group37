#name: JSDoc

#on:
  #push:
    #branches:
      #- jsdoc-testing
#jobs:
  #deploy:
    #runs-on: ubuntu-latest
    #steps:
      #- name: Checkout code
      #  uses: actions/checkout@v3          
      #- name: run properties
       # run: |
        #  npm i -g jsdoc
        #  npm fund
         # jsdoc ./admin/testingCI/* ./out
      #- name: JSDoc Action
      # You may pin to the exact commit or the version.
      # uses: andstor/jsdoc-action@06eafccd94e3fedcbf4ff0a8f23d775c169efdf2
      #  uses: andstor/jsdoc-action@v1.2.1
      #  with:
        # Source directory to build documentation from
        # Output folder for the generated documentation
      #    output_dir: ./admin/testingCI/documentations
      
      
      #- run: jsdoc ./admin/testingCI/* ./ 
  
     # - name: Add & Commit
     #   uses: EndBug/add-and-commit@v9.1.1
      #  with:
       #   add: 'out'
       #   message: "Autogenerated Docs"
          
        #node version  
      #- name: Build
      #  uses: andstor/jsdoc-action@v1.2.1
      #  with:
      #    source_dir: ./admin/testingCI/
        
      #- name: Deploy
      #  uses: peaceiris/actions-gh-pages@v3
      #  with:
      #    deploy_key: ${{secrets.S6KrXtaPgLC1L4KB7tkPcEcEDzWRWO2hVKNIcmmaygM}}
      #    publish_dir: ./out

name: JSDOC
on: [pull_request, push]
jobs:

  generate:
    runs-on: ubuntu-latest
    env:
      EVENT_NUMBER: ${{ github.event.number }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        
      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: 18.x
      - name: jsDocs
        run: |
          npm install
          npm run doc
      - name: Add & Commit
        uses: EndBug/add-and-commit@v9.1.1
        with:
          add: 'documentations'
          message: "Autogenerated Docs"
